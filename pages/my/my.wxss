.log-list {
  display: flex;
  flex-direction: column;
  padding: 40rpx;
}
.log-item {
  margin: 10rpx;
}
.hr {
  width: 100%;
  height: 10px;
  background-color: #f4f5f6;
}
.item {
  background: white;
  display: flex;
  flex-direction: row;
}
.title {
  padding-top: 15px;
  padding-bottom: 15px;
  padding-left: 15px;
  /* padding 简写属性在一个声明中设置所有内边距属性(顺序依次是上右下左) */
  font-size: 15px;
}
.detail2 {
  font-size: 15px;
  position: absolute;
  right: 10px;
  height: 50px;
  line-height: 50px;
  color: #888;
}
.line {
  border: 1px solid #cccccc;
  opacity: 0.2;
  /* opacity 属性设置元素的不透明级别,value	规定不透明度。从 0.0 （完全透明）到 1.0（完全不透明）。 */
}